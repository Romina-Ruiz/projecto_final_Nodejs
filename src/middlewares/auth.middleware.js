export const auth = (req, res, next) => {
  // Middleware de autenticación mínimo: permite todas las solicitudes.
  // Reemplazar con lógica real (verificar token) cuando esté disponible.
  next();
};

